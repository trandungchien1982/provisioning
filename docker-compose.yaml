version: "3.8"

services:
  filebrowser:
    image: filebrowser/filebrowser:v2-s6
    container_name: filebrowser
    ports:
      - "80:80"
    volumes:
      - /root/test-123:/srv
      - ./data:/database
      - ./config:/config
    restart: always