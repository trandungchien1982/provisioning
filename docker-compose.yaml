version: '3.9'

services:
  coredns:
    image: coredns/coredns:1.14.2
    container_name: coredns
    restart: always

    ports:
      - "53:53/udp"
      - "53:53/tcp"

    volumes:
      - ./Corefile:/Corefile
      - ./zones:/zones

    command: -conf /Corefile
