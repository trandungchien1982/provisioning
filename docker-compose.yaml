version: "3.8"

services:
  sftp:
    image: atmoz/sftp:debian
    container_name: sftp-server
    restart: always
    volumes:
      # The folder name /home/admin/upload belongs to the user/pass : admin/admin
      - ./host_sftp/upload:/home/admin/upload
    ports:
      - "2222:22"
    command: admin:admin:1001
