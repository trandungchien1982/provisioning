version: "3.8"

services:
  navidrome:
    image: deluan/navidrome
    ports:
      - "4533:4533"
    volumes:
      - ./music:/music
      - ./navidrome:/data

    environment:
      # Allow share Album/Tracks/... without login ...
      ND_ENABLESHARING: "true"

    restart: always